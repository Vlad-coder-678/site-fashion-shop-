import React from "react";

import {
  ContactsBlock,
  CallBackBlock,
} from "../blocks";

const ContactsPage = () => {
  return (
    <div className="page">
      <ContactsBlock />
      <CallBackBlock />
    </div>
  );
};

export default ContactsPage;
